# Test defaults for the app module. Redirect the operational log file under target/ (which is
# git-ignored and cleaned) so a full-context test boot never writes a logs/ directory into the
# module. Tests that assert on the log file itself override this with their own temp directory.
tapstate.log.dir=target/test-logs
