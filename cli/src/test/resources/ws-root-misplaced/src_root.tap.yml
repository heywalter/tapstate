# CLI smoke fixture (misplaced): a valid source sitting directly at the workspace root, not under a
# source/ kind directory. Its parent dir is the workspace root name, which never equals a kind.
version: tapstate/v1
kind: source
id: src_root
connector: mysql
config: { host: 10.0.0.7, username: u, password: p }
