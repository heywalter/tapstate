# Batch deliberately declares the same top-level id twice across files (see the sibling
# file): workspace top-level ids share one namespace and must be unique (§2/F8).
version: tapstate/v1
kind: source
id: src_orders
connector: mysql
config: { host: 10.10.0.5, username: cdc_user, password: Ord_2026 }
mode: cdc
tables: [ orders ]
