# Copy to .env to override the demo defaults. Without a .env the stack still runs, with the
# first administrator created as admin/admin -- fine for a local demo, unfit for anything reachable
# by others. The quickstart script writes a .env with a generated password rather than this default.
TAPSTATE_ADMIN_USER=admin
TAPSTATE_ADMIN_PASSWORD=admin
