/**
 * Convergence-side controller host: placement / ownership / failover.
 *
 * <p>Placeholder package reserving the module; the watch-desired / converge-actual
 * controllers are added when scheduling lands. Rule R4: this module depends on core + spi
 * (and the Hazelcast fork) only — never on an adapter.
 */
package io.tapstate.runtime.scheduler;
